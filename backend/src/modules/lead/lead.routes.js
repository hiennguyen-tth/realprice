'use strict';

// Lead routes are mounted inline within listing.routes.js (POST /:id/contact).
// This file is a placeholder for any standalone lead management routes (e.g. admin).

const { Router } = require('express');

const router = Router();

module.exports = router;
